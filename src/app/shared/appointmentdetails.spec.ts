import { Appointmentdetails } from './appointmentdetails';

describe('Appointmentdetails', () => {
  it('should create an instance', () => {
    expect(new Appointmentdetails()).toBeTruthy();
  });
});
