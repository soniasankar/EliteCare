import { AMedicalRecordPrescriptionDto } from './amedical-record-prescription-dto';

describe('AMedicalRecordPrescriptionDto', () => {
  it('should create an instance', () => {
    expect(new AMedicalRecordPrescriptionDto()).toBeTruthy();
  });
});
